# 协议定位

## 1. 协议是什么

AGW UI Interaction Protocol 定义的是前端应用如何与智能体平台通信，而不是前端直接与某个具体 Agent 的私有协议。

这里的 Agent Platform 是协议层的统一服务端主体。Gateway 可以是其中一种部署模式，但协议本身不要求中间必须存在 Gateway。

平台通常承担三类职责：

- 对外暴露统一的 `/api/*` HTTP API
- 把请求路由到具体运行实例、单智能体或多智能体协作链路
- 通过统一的实时传输把过程和结果持续回传给前端

推荐理解方式是：前端只对接协议层的 Agent Platform，至于内部是否以 Gateway、Runner 或其他服务编排实现，不影响协议本身。

## 2. 默认阅读主线

如果你第一次阅读这套文档，建议按下面顺序：

1. 本页：理解协议主体与目标
2. [术语与边界](02-概览-术语边界与架构主线.md)：理解 HTTP / SSE / WebSocket 的分层
3. [架构与交互图](02-概览-术语边界与架构主线.md)：理解主流程与参与方
4. [HTTP API](10-协议数据-HTTP核心运行接口.md)、[SSE 事件模型](13-协议数据-SSE传输与请求运行事件.md)、[WebSocket 协议](15-协议数据-WebSocket传输协议.md)
5. [交互时序图](40-交互时序-编号化主图与资产引用.md) 与 [接入用例](30-接入用例-基础问答与工具调用.md)

## 3. 传输方式

当前协议的默认讲解主路径仍然是：

- 公开 HTTP API
- `POST /api/query` 返回 live SSE
- 运行中通过 `submit / steer / interrupt` 继续交互

如果服务端启用 WebSocket 扩展，也可以通过 `/ws` 承载同一套业务语义；但这不会改变 HTTP API 和事件语义本身，只是新增一种传输方式。

## 4. 文档分区职责

- `01-02`：讲协议定位、术语边界、架构和参与方
- `10-16`：讲 HTTP、SSE、WebSocket 与共享数据模型
- `20-22`：讲 HITL、运行控制、资源与产物交互
- `30-33`：讲接入用例
- `40`：讲时序图和 SVG 资产引用
- `50-51`：讲平台扩展与非核心 WS route
- `60`：讲 SDK、测试项目、Demo 和视觉资源
- `visuals/`：讲辅助理解页面，不属于正式规范正文
